export enum UserError {
  USER_NOT_FOUND = 'User not found !',
}

export enum Order {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum UserType {
  ADMIN = 'admin',
  EDITOR = 'editor',
  MODERATOR = 'moderator',
}
